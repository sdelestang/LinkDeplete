% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ModelCode.R
\name{solve_target_catch}
\alias{solve_target_catch}
\title{Solve for the catch achieving a target depletion level}
\usage{
solve_target_catch(
  target_depletion,
  B_end_prev,
  B0_end_prev,
  R,
  M,
  catch_split,
  fish_steps,
  rec_ts,
  TS = 12,
  tol = 0.01
)
}
\arguments{
\item{target_depletion}{Target depletion ratio (e.g. 0.40 for 40\\%
of unfished biomass).}

\item{B_end_prev}{End-of-season fished biomass from the previous season.}

\item{B0_end_prev}{End-of-season unfished biomass from the previous season.}

\item{R}{Recruitment biomass to be added at time step \code{rec_ts}.}

\item{M}{Instantaneous natural mortality per time step.}

\item{catch_split}{Numeric vector of proportional catch allocation
across fishing time steps.}

\item{fish_steps}{Integer vector of time steps in which fishing occurs.}

\item{rec_ts}{Integer time step at which recruitment enters.}

\item{TS}{Total number of time steps per season. Default 12.}

\item{tol}{Tolerance passed to \code{\link[stats]{uniroot}}.
Default 0.01.}
}
\value{
A list as returned by \code{\link[stats]{uniroot}}, where
\code{$root} is the total catch that achieves the target depletion.
}
\description{
Wrapper around \code{\link{project_depletion_end}} that uses
\code{\link[stats]{uniroot}} to find the total seasonal catch
producing a specified end-of-season depletion ratio (B/B0).
}
\details{
The search interval runs from zero catch to \code{B_end_prev + R},
which represents the theoretical maximum removable biomass. If the
target depletion is unachievable within this range (e.g. the stock
is already below the target with zero catch), \code{uniroot} will
error.
}
\seealso{
\code{\link{project_depletion_end}}, \code{\link{DepleteModel}}
}
